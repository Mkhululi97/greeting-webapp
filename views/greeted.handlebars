<div class="container">
  <div class="nav">
    <a href="/">HOME</a>
    <a class="user-navi" href="/greeted">USERS</a>
  </div>
  <h1 class="primary-heading-greetings">GREETINGS LOVELY PEOPLE👋🏽</h1>
  <div class="table-div">
    <table>
      <tr>
        <th class="column-heading">User</th>
      </tr>
      {{#each users}}
        <tr>
          <td><a href="/counter/{{this.user_name}}">{{this.user_name}}</a></td>
        </tr>
      {{/each}}
      <tr>
        <td class="user-msg">{{greetedTimesMsg}}</td>
      </tr>
    </table>
  </div>
</div>